{
  "name": "app",
  "scripts": {
      "build": "esbuild app/javascript/application.js --bundle --outdir=app/assets/builds --loader:.js=jsx",
      "build:watch": "npm run build -- --watch"

  },
  "private": true,
  "devDependencies": {
    "esbuild": "^0.25.5"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
} 
